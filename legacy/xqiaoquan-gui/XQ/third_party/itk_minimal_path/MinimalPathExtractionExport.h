#pragma once

// XQ compiles the vendored optimizer directly into one development target, so
// the upstream module's generated shared-library export macro is empty here.
#define MinimalPathExtraction_EXPORT
