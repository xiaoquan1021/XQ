{"_note": "No external spec/research dependency. All root causes, signatures, and edit sites are written into prd.md/design.md/implement.md. Worker context is delivered via the send message body (per memory channel-spawn-file-jsonl-hangs-worker: NEVER spawn with --jsonl/--file, it hangs the worker); the send body gives absolute paths to the three task docs for the worker to Read."}
